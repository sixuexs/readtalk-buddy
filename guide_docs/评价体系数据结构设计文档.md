# 阅谈智伴 · 评价体系数据结构设计文档

> **版本**：终态合并版（含 2026-07 修订：3D→5D 评分维度升级、contact 表字段修订、MongoDB 侧 Evaluation/Profile 实现记录）。
> **适用范围**：社交能力评估 / 书友关系 / 交流记录 这条核心数据链（即"评价体系 + 关系图谱"的数据骨架）。
> **不在范围**：`user` 账号表（见 §0.3）、前端交互、AI prompt 文本。

---

## 0. 阅读与使用约定

### 0.1 库划分原则（与申报书技术方案一致）
- **MySQL**：需聚合 / 排序 / 关联 / 强 schema 的数据 → 评估分、书友档案、交流元数据。
- **MongoDB**：嵌套文档 / 长文本流 / 追加式 → 对话逐句明细（`conversation_detail`）、对话主文档（`conversations`）、用户画像（`user_profile`）。
- 原则：**存事实、算派生、评整篇** 三层分离。整篇级 AI 评分与点评进 MySQL；逐句事实与逐句标注进 MongoDB；派生量（所属环、预警级别、交流次数）一律不落库，查询/规则层现算。

### 0.2 全系统命名规则（维度枚举）⚠️ 三方（后端 Java / 前端 TS / AI prompt）必须统一

社交能力共 **5 维**。IDL（DB 列 / JSON 键）使用 snake_case，代码层（Java / TypeScript）使用 camelCase：

| Java/TS 字段名 | DB 列名 (`score_xxx`) | 短名（JSON key / rubric key） | 含义 |
|---|---|---|---|
| `clarity` | `score_clarity` | `clarity` | 表达清晰度 |
| `logicality` | `score_logicality` | `logicality` | 逻辑思辨力 |
| `empathyListening` | `score_empathy_listening` | `empathy_listening` | 共情与倾听 |
| `interactivity` | `score_interactivity` | `interactivity` | 互动积极性 |
| `relaxation` | `score_relaxation` | `relaxation` | 情绪松弛度 |

> 雷达图 / 折线图 / AI 写回 / 前端渲染全部按此对齐，禁止起别名。

### 0.3 user 表边界声明 ⚠️
`user` 账号表**不在本文档定义**，由登录注册模块负责。本文档所有 `user_id` 均逻辑外键引用 `user.id`。
- **最小占位约定**：`user.id` = `BIGINT UNSIGNED`，自增主键。
- 本文档**不建物理外键**，全部逻辑外键，应用层保证完整性。

### 0.4 通用约定
- 字符集 `utf8mb4` / 排序 `utf8mb4_unicode_ci` / 引擎 `InnoDB`。
- 所有 `DATETIME` **全系统统一时区**（全 UTC 或全本地，禁止混用，否则折线图跨天错位）。
- 软删表的所有读查询带 `deleted_at IS NULL`。

---

## 1. 实体关系总览

### 数据流总图

```
 MySQL                             MongoDB (明细/标注层)           MongoDB (运行时层)
─────                             ──────────────────           ─────────────────
                                                                   
 evaluation_record ◀───(将来写入)── conversations.Evaluation ──── SimulationService
   (MySQL 查询源)      (写路径未实现)    (对话原始评分 / 当前唯一写源)   scoreConversation()
       │                                     │                          
       │ (同 user_id)                        │ messages[] (运行消息流)
       ▼                                     ▼                                      
 contact ◀── (同步投影) ──── ContactDocument               ──── RelationTools / SimulationTools
   (MySQL 查询源)           (MongoDB 写主 / Agent 读写)          (现有 Agent, 不可动)
       ▲                                         
       │ (同 user_id)                          
       │                                         
 interaction_meta ──biz_id──▶ conversation_detail (逐句标注, 复盘翻译写入) 
   (MySQL 元数据)              messages[] 来自 conversations 的逐句翻译+标注
                               ↓
                               亲密度计算(沙龙分支)
                                   
                               user_profile (画像/异步)
                                    │
                                    ▼
                               ScoreRecord[]: 历次评分快照
                               avgXxx: 5 维均值
```

**图例**：✅ = 已实现并运行；❌ = 设计目标但写逻辑未实现；🔄 = 链路已定义但翻译/调度任务未实现（conversation_detail 当前无数据）

**图注（关键关系线）**：

| 关系 | 类型 | 当前状态 |
|------|------|---------|
| `conversations.Evaluation` → `evaluation_record` | **评分持久化** | ❌ 写逻辑未实现（见 §9.4 写路径差异） |
| `conversations.messages[]` → `conversation_detail.messages[]` | **消息转换 + 标注** | 🔄 复盘完成后翻译写入, annotation AI 补充 |
| `ContactDocument`(Mongo) → `contact`(MySQL) | **同步投影** | ✅ ContactSyncListener 已实现, 单向 M→S |
| `ConversationDetailDocument` → 亲密度计算 | **沙龙对子聚** | 🔄 调度任务未实现（挂起） |
| `evaluation_record` → 雷达图/折线图 | **SQL 查询** | 🔄 数据写满后启用, 当前前端读 conversations |

```mermaid
erDiagram
    USER ||--o{ EVALUATION_RECORD : "user_id"
    USER ||--o{ CONTACT : "user_id"
    USER ||--o{ INTERACTION_META : "user_id"
    CONTACT ||--o{ EVALUATION_RECORD : "related_contact_id (1v1)"
    CONTACT ||--o{ INTERACTION_META : "related_contact_id (1v1)"
    INTERACTION_META ||--|| EVALUATION_RECORD : "interaction_id"
    INTERACTION_META ||--|| CONVERSATION_DETAIL : "biz_id = _id"
    CONVERSATIONS ||--o{ CONVERSATION_DETAIL : "messages[] 转换翻译"
    CONVERSATIONS ||--o{ EVALUATION_RECORD : "Evaluation 持久化(将来)"
```

> **ER 说明**：`CONVERSATIONS` 代表 MongoDB 的 `conversations` 集合（现行运行时），关系线为虚线表示"写路径未实现"。`conversations.messages[]` → `conversation_detail.messages[]` 为转换翻译关系，由复盘流程触发。

> 补充：`interaction_meta.participant_contact_ids[]` → `contact.id`（仅沙龙，非物理外键）。
> 关联闭环：`contact.id` 是 `evaluation.related_contact_id` 与 `interaction_meta.related_contact_id / participant_contact_ids` 的共同指向目标。

---

## 2. MySQL · evaluation_record（评估记录，中枢表）

**定位**：雷达图 / 折线图 / 亲密度"深度·质量"分（仅 1 对 1）的数据源。一次复盘或一次模拟 = 一行。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 主键 |
| user_id | BIGINT UNSIGNED | NOT NULL | 被评估用户 → user.id |
| source | TINYINT | NOT NULL | 1=real 真实复盘 / 2=simulation 情景模拟 |
| scene_type | VARCHAR(32) | NOT NULL | salon / one_on_one / library_encounter / sim_xxx … |
| score_clarity | TINYINT UNSIGNED | NOT NULL | 0–100 |
| score_logicality | TINYINT UNSIGNED | NOT NULL | 0–100 |
| score_empathy_listening | TINYINT UNSIGNED | NOT NULL | 0–100 |
| score_interactivity | TINYINT UNSIGNED | NOT NULL | 0–100 |
| score_relaxation | TINYINT UNSIGNED | NOT NULL | 0–100 |
| score_overall | TINYINT UNSIGNED | NOT NULL | AI 加权总分 0–100，第一版等权 = ROUND(AVG(五维)) |
| ai_analysis_text | TEXT | NULL | AI 整篇文字点评 |
| self_relative | JSON | NULL | 用户每维相对上次：`{"clarity":"up"\|"flat"\|"down", ...}` |
| self_state | VARCHAR(32) | NULL | 状态标签（**词表挂起**，见 §8） |
| self_comment | VARCHAR(500) | NULL | 用户评语，选填 |
| related_contact_id | BIGINT UNSIGNED | NULL | **one_on_one 填 contact.id；salon / simulation 为 NULL** |
| interaction_id | BIGINT UNSIGNED | NOT NULL | → interaction_meta.id（real 与 sim 均非空） |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 评估时间 |
| updated_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | |

**索引**：`idx_user_time(user_id, created_at)`、`idx_user_source(user_id, source)`、`idx_user_contact(user_id, related_contact_id)`。

```sql
CREATE TABLE evaluation_record (
  id                      BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  user_id                 BIGINT UNSIGNED NOT NULL                COMMENT '被评估用户->user.id',
  source                  TINYINT         NOT NULL                COMMENT '1=real 2=simulation',
  scene_type              VARCHAR(32)     NOT NULL                COMMENT 'salon/one_on_one/library_encounter/sim_xxx',
  score_clarity           TINYINT UNSIGNED NOT NULL               COMMENT '表达清晰度0-100',
  score_logicality        TINYINT UNSIGNED NOT NULL               COMMENT '逻辑思辨力0-100',
  score_empathy_listening TINYINT UNSIGNED NOT NULL               COMMENT '共情与倾听0-100',
  score_interactivity     TINYINT UNSIGNED NOT NULL               COMMENT '互动积极性0-100',
  score_relaxation        TINYINT UNSIGNED NOT NULL               COMMENT '情绪松弛度0-100',
  score_overall           TINYINT UNSIGNED NOT NULL               COMMENT 'AI加权总分0-100',
  ai_analysis_text        TEXT            NULL                    COMMENT 'AI整篇文字点评',
  self_relative           JSON            NULL                    COMMENT '每维相对上次 up/flat/down,键名=维度短名',
  self_state              VARCHAR(32)     NULL                    COMMENT '状态标签(词表挂起,见文档§8)',
  self_comment            VARCHAR(500)    NULL                    COMMENT '用户评语',
  related_contact_id      BIGINT UNSIGNED NULL                    COMMENT 'one_on_one填contact.id; salon/simulation为NULL',
  interaction_id          BIGINT UNSIGNED NOT NULL                COMMENT '->interaction_meta.id',
  created_at              DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at              DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  KEY idx_user_time    (user_id, created_at),
  KEY idx_user_source  (user_id, source),
  KEY idx_user_contact (user_id, related_contact_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
  COMMENT='社交能力评估记录:雷达图/折线图/亲密度(1v1深度质量)数据源';
```

**要点**：
- `score_overall` 第一版等权 = `ROUND(AVG(五维))`，**代码注释必须标明**；将来改加权须迁移历史 overall 或接受折线断层，禁止静默改口径。
- 5 维存整数 TINYINT 是刻意降噪（AI 单次 ±3~5 波动）；移动平均的小数在展示层算，不落库。
- `divergence_flags`（AI 分升降 vs 用户相对自评的背离）**不存表**，详情页查最近几条现算。
- salon 时 `related_contact_id=NULL`：5 维分是用户**整场**表现，进用户雷达图，**不**进任何个人亲密度。

**Java 实现**：`com.backend.entity.EvaluationRecord`（JPA Entity，17 字段，含 `@GeneratedValue` 自增主键 + `@Column(columnDefinition = "JSON")` 的 `selfRelative`）。对应 repository：`com.backend.repository.jpa.EvaluationRecordRepository`。

---

## 3. MySQL · contact（书友档案）🔴 已修订

**定位**：关系图谱 / 预警 / 通讯录 / 维护建议 的数据源。"我的一个书友" = 一行。

**修订内容（相对初版）**：
1. `display_name` → `name`（保留现有代码命名习惯，含义不变）
2. 新增 `relation_type`（社交身份，如家人/朋友/同事/同学）—— 与 `category`（书友圈扇区）**正交两维度并存**
3. 新增 `personality`、`interests`(JSON)、`labels`(JSON) —— 源自现有 MongoDB `ContactDocument` 的字段迁移（Plan A）

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 主键，即他表 related_contact_id 指向值 |
| user_id | BIGINT UNSIGNED | NOT NULL | 档案拥有者 → user.id |
| name | VARCHAR(64) | NOT NULL | 显示名/备注，用户自起，同名不拦 |
| avatar_url | VARCHAR(500) | NULL | 头像，图谱节点渲染用 |
| linked_user_id | BIGINT UNSIGNED | NULL | 对方若为本系统用户则 → user.id；**第一版全 NULL，预留** |
| category | VARCHAR(32) | NOT NULL DEFAULT 'other' | 关系分类 = 图谱扇形 = 书友圈，如"科幻文学""历史人文"（**词表挂起**，见 §8） |
| relation_type | VARCHAR(32) | NOT NULL DEFAULT 'other' | 社交身份，如"家人""朋友""同事""同学"（与 category 正交，两维度并存） |
| source_scene | VARCHAR(128) | NULL | 认识来源，如"《XX》读书会 2026-07" |
| personality | VARCHAR(200) | NULL | 性格描述，如"幽默风趣社牛""沉稳内敛观察者" |
| interests | JSON | NULL | 兴趣爱好数组，如 `["阅读","咖啡","摄影"]` |
| labels | JSON | NULL | 关系/身份标签数组，如 `["好友","大学同学"]` |
| intimacy_score | DECIMAL(5,2) | NOT NULL DEFAULT 0.00 | 亲密度连续分 0–100，**物化快照** |
| last_contact_time | DATETIME | NULL | 上次交流时间，时效衰减用 |
| warning_dismissed_at | DATETIME | NULL | "暂不提醒"时间，冷却期内规则跳过 |
| deleted_at | DATETIME | NULL | 软删 |
| created_at / updated_at | DATETIME | NOT NULL | 标准 |

**索引**：`UNIQUE uk_user_linked(user_id, linked_user_id)`、`idx_user_list(user_id, deleted_at, updated_at)`、`idx_user_intimacy(user_id, intimacy_score)`、`idx_user_last(user_id, last_contact_time)`。
> `uk_user_linked`：linked_user_id 为 NULL 时 InnoDB 不去重，正好允许多个手动档案。

```sql
CREATE TABLE contact (
  id                   BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  user_id              BIGINT UNSIGNED NOT NULL                COMMENT '档案拥有者->user.id',
  name                 VARCHAR(64)     NOT NULL                COMMENT '显示名/备注',
  avatar_url           VARCHAR(500)    NULL                    COMMENT '头像url',
  linked_user_id       BIGINT UNSIGNED NULL                    COMMENT '对方若是本系统用户->user.id,预留,第一版全NULL',
  category             VARCHAR(32)     NOT NULL DEFAULT 'other' COMMENT '关系分类=图谱扇形=书友圈(词表挂起,见§8)',
  relation_type        VARCHAR(32)     NOT NULL DEFAULT 'other' COMMENT '社交身份:家人/朋友/同事/同学(与category正交)',
  source_scene         VARCHAR(128)    NULL                    COMMENT '认识来源',
  personality          VARCHAR(200)    NULL                    COMMENT '性格描述',
  interests            JSON            NULL                    COMMENT '兴趣爱好数组',
  labels               JSON            NULL                    COMMENT '关系/身份标签数组',
  intimacy_score       DECIMAL(5,2)    NOT NULL DEFAULT 0.00   COMMENT '亲密度连续分0-100,物化快照',
  last_contact_time    DATETIME        NULL                    COMMENT '上次交流时间,衰减用',
  warning_dismissed_at DATETIME        NULL                    COMMENT '暂不提醒时间,冷却期内跳过',
  deleted_at           DATETIME        NULL                    COMMENT '软删',
  created_at           DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at           DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  UNIQUE KEY uk_user_linked (user_id, linked_user_id),
  KEY idx_user_list     (user_id, deleted_at, updated_at),
  KEY idx_user_intimacy (user_id, intimacy_score),
  KEY idx_user_last     (user_id, last_contact_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
  COMMENT='书友档案:关系图谱/预警/通讯录数据源';
```

**要点**：
- **两维度并存**：`category` = 书友圈扇区（图谱用），`relation_type` = 社交身份（列表/通讯录用），互不替代。
- **物化 vs 不物化**：`intimacy_score` 物化（时变量，靠定时任务保鲜）；**所属环 ring、预警级别 warning_level、交流次数/频率一律不存**（ring 是 score 的瞬时纯函数，阈值=代码常量，封测改常量即可）。
- 环分桶建议初值（**代码常量，可配置**）：内环 ≥70 / 二环 40–69 / 外环 <40。
- 软删：所有读带 `deleted_at IS NULL`。

**Java 实现**：`com.backend.entity.ContactEntity`（JPA Entity，`@Entity(name="contact")`，17 字段，`interests`/`labels` 为 `@Column(columnDefinition="JSON")`，`intimacy_score` 为 `BigDecimal`）。对应 repository：`com.backend.repository.jpa.ContactJpaRepository`。

**MongoDB → MySQL 同步**：`com.backend.service.ContactService.saveOrUpdate(ContactDocument, Long)` 负责从现有 MongoDB `ContactDocument` 全量映射到 MySQL `ContactEntity`。通过 `ContactSyncListener`（`@EventListener(ContactSavedEvent.class)`）解耦触发，MySQL 不可用时 grace 降级（log.warn，不阻断主流程）。

**前端类型**：`frontend/src/types/relation.ts` → `RelationContact` 接口，含 `category?: string`。mock 数据 7 条已按书友圈分类。

**迁移阶段说明（按字段分写主）**：当前 contact 数据处于**按字段分写主的过渡架构**：

| 字段类别 | 写主 | 同步方向 | 说明 |
|---|---|---|---|
| 档案字段（白名单）| MongoDB `ContactDocument` | M→S 单向同步 | name, avatarUrl, relationType, personality, interests, labels — Agent 在 MongoDB 侧读写 |
| 度量字段 | MySQL `ContactEntity` | 无同步（各自独立） | intimacy_score, last_contact_time, warning_dismissed_at — 由 §6 评分事务和 §7 定时任务在 MySQL 侧计算 |
| 元数据字段 | MySQL `ContactEntity` | 由 JPA/DB 管理 | id(自增), user_id(入参), created_at/updated_at(DB默认), deleted_at(软删) |

- **阶段一（当前）**：按字段分写主。`saveOrUpdate(ContactDocument)` 只同步档案字段（白名单），不触碰度量字段。Agent 继续读写 MongoDB，定时任务和评分事务在 MySQL 侧独立计算度量值。
- **阶段二（后续）**：Agent 读写目标逐步切到 ContactEntity，所有字段收敛到 MySQL 写主。
- **阶段三（远期）**：ContactDocument 降级为只读缓存或废弃。

> ⚠️ `category` 虽在 contact 表，但 ContactDocument 无此字段，故不参与 M→S 同步。由 MySQL 侧（用户图谱操作）维护，新增时 DB 默认 'other'。
> ⚠️ `linkedUserId` 写主 = MySQL（用户匹配系统用户时设），ContactDocument 无此字段，saveOrUpdate 永不触碰。
> ⚠️ `sourceScene` 写主 = MySQL（用户添加书友时填"认识来源"），ContactDocument 无此字段，不参与 M→S 同步，saveOrUpdate 永不触碰。

**阶段一已知限制**：M→S 同步以 `name` 为 upsert 查找键。name 不唯一且用户可改，存在重复记录/错配风险（改 name 后同步可能新增重复行）。demo 阶段数据量小、name 不常改，错配概率低，暂挂起为已知限制。阶段二切 MySQL 写主时，须引入稳定关联键（如新增 `mongo_ref_id` 存 `ContactDocument._id`）替代 name 做 upsert。

---

## 4. MySQL · interaction_meta（交流记录元数据）

**定位**：亲密度"时效 / 频率"来源；评估跳回原始对话的桥梁。一次交流 = 一行（real 与 sim 统一，靠 type 区分）。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 主键，evaluation.interaction_id 指向它 |
| biz_id | VARCHAR(64) | NOT NULL, UNIQUE | 业务幂等键 = Mongo 明细 `_id`，**应用层预生成** |
| user_id | BIGINT UNSIGNED | NOT NULL | 所属用户 → user.id |
| type | TINYINT | NOT NULL | 1=real / 2=simulation |
| scene_type | VARCHAR(32) | NOT NULL | 冗余一份免 join |
| related_contact_id | BIGINT UNSIGNED | NULL | **one_on_one → contact.id；salon / sim → NULL** |
| participant_contact_ids | JSON | NULL | **沙龙在场书友 id 列表 `[c1,c2,…]`；one_on_one / sim 为 NULL** |
| started_at | DATETIME | NOT NULL | 交流开始 |
| ended_at | DATETIME | NULL | 交流结束；衰减用 `COALESCE(ended_at, started_at)`；复盘完成回填 |
| duration_seconds | INT UNSIGNED | NULL | 时长，分析用，不进亲密度核心公式 |
| participant_count | TINYINT UNSIGNED | NULL | 参与人数 |
| process_status | TINYINT | NOT NULL DEFAULT 0 | 0=明细待同步 / 1=全部完成 / 2=明细同步失败(补偿重试) |
| created_at / updated_at | DATETIME | NOT NULL | 标准 |

**索引**：`UNIQUE uk_biz_id(biz_id)`、`idx_user_contact_time(user_id, related_contact_id, started_at)`、`idx_user_started(user_id, started_at)`、`idx_user_type(user_id, type)`、`idx_status(process_status, updated_at)`。

```sql
CREATE TABLE interaction_meta (
  id                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  biz_id                VARCHAR(64)     NOT NULL                COMMENT '业务幂等键=Mongo明细_id,应用层预生成',
  user_id               BIGINT UNSIGNED NOT NULL                COMMENT '所属用户->user.id',
  type                  TINYINT         NOT NULL                COMMENT '1=real 2=simulation',
  scene_type            VARCHAR(32)     NOT NULL                COMMENT 'salon/one_on_one/library_encounter/sim_xxx',
  related_contact_id    BIGINT UNSIGNED NULL                    COMMENT 'one_on_one->contact.id; salon/sim->NULL',
  participant_contact_ids JSON          NULL                    COMMENT '沙龙在场书友id列表[salon填,one_on_one/sim为NULL]',
  started_at            DATETIME        NOT NULL                COMMENT '交流开始',
  ended_at              DATETIME        NULL                    COMMENT '交流结束,衰减用,复盘时回填',
  duration_seconds      INT UNSIGNED    NULL                    COMMENT '时长,分析用',
  participant_count     TINYINT UNSIGNED NULL                   COMMENT '参与人数',
  process_status        TINYINT         NOT NULL DEFAULT 0      COMMENT '0待同步1完成2同步失败',
  created_at            DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at            DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  UNIQUE KEY uk_biz_id (biz_id),
  KEY idx_user_contact_time (user_id, related_contact_id, started_at),
  KEY idx_user_started      (user_id, started_at),
  KEY idx_user_type         (user_id, type),
  KEY idx_status            (process_status, updated_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
  COMMENT='交流记录元数据:亲密度时效/频率来源,评估跳回对话的桥梁';
```

**要点**：
- `biz_id` 不可省：MySQL 自增 id 在 commit 前不可预生成，跨库幂等键必须应用层预生成；Mongo 文档 `_id` 即用 `biz_id`。
- `related_contact_id`（单值，走索引，服务 1 对 1 高频列表）与 `participant_contact_ids`（JSON，服务沙龙低频聚合）**职责正交，非冗余**。
- `process_status` 三态：meta 行**一定存在**（MySQL 先成），补偿任务扫 `status IN (0,2)` 重发 Mongo 即可。

**Java 实现**：`com.backend.entity.InteractionMeta`（JPA Entity，Java 字段 `source` 映射 DB 列 `type`，这是命名层面的适配，非数据模型差异）。对应 repository：`com.backend.repository.jpa.InteractionMetaRepository`。

---

## 5. MongoDB · conversation_detail（对话逐句明细）

**定位**：原始对话事实 + 逐句标注。**不放整篇 summary**（整篇级产出在 `evaluation.ai_analysis_text`，避免语义撞车）。real 与 sim 合并同一集合，靠 `type` 区分。

| 字段 | 类型 | 说明 |
|---|---|---|
| _id | string | = `meta.biz_id`，主键 + 幂等 |
| user_id | long | 冗余，免回 mysql |
| type | int | 1 real / 2 sim，冗余 |
| scene_type | string | 冗余 |
| participants | array | 对方列表 `[{refId, role, tag}]`；role=contact→refId=contact.id,tag=null；role=sim_character→refId=null,tag=性格标签。**user 自己不列** |
| sim_context | object\|null | 模拟专属 `{scenario_id, character_persona}`；real 为 null |
| messages | array | 逐句流，结构见下 |
| created_at / updated_at | datetime | |

**messages[] 单条**：

| 字段 | 类型 | 说明 |
|---|---|---|
| seq | int | 句序 |
| ts | datetime\|null | 该句时间 |
| speaker_role | string | user / contact / sim_character / unknown |
| speaker_ref | long\|null | contact_id 或 sim 标识；user / unknown 为 null |
| content | string | 该句文本 |
| annotation | string\|null | 标注 JSON 字符串（`{keywords[], emotion, intent, responds_to_seq}`），AI/规则可选填，标不了则 null |

> `annotation.responds_to_seq` 支撑"共情与倾听"客观指标；`keywords` 支撑申报书"关键词检索语料库"。两者 nullable，**不阻塞写入**。
> `speaker_role=unknown`：沙龙嘈杂识别不出的句子，**不计入任何 contact 亲密度**。

```js
db.createCollection("conversation_detail");
db.conversation_detail.createIndex({ user_id: 1, created_at: 1 });
```

**示例文档（salon，体现多人 + 逐句归属 + unknown）**：
```json
{
  "_id": "BZ202607220001",
  "user_id": 1001, "type": 1, "scene_type": "salon",
  "participants": [
    { "refId": 55, "role": "contact", "tag": null },
    { "refId": 78, "role": "contact", "tag": null }
  ],
  "sim_context": null,
  "messages": [
    { "seq": 1, "ts": "2026-07-22T19:01:00Z", "speaker_role": "contact", "speaker_ref": 55,
      "content": "我觉得主角的逃避其实是种自我保护。",
      "annotation": "{\"keywords\":[\"主角\",\"逃避\",\"自我保护\"],\"emotion\":\"平静\",\"intent\":\"观点陈述\",\"responds_to_seq\":null}" },
    { "seq": 2, "ts": "2026-07-22T19:01:20Z", "speaker_role": "user", "speaker_ref": null,
      "content": "我同意，但书里第三章他明明有机会面对。",
      "annotation": "{\"keywords\":[\"第三章\",\"面对\"],\"emotion\":\"认同\",\"intent\":\"回应+补充\",\"responds_to_seq\":1}" },
    { "seq": 3, "ts": "2026-07-22T19:01:40Z", "speaker_role": "unknown", "speaker_ref": null,
      "content": "（环境嘈杂未识别发言者）", "annotation": null }
  ],
  "created_at": "2026-07-22T19:05:00Z", "updated_at": "2026-07-22T19:05:00Z"
}
```

**Java 实现**：`com.backend.document.ConversationDetailDocument`（`@Document("conversation_detail")`，内嵌 `Participant` + `MessageItem` 静态内部类，`annotation` 字段在 Java 层为 `String` 类型存储完整 JSON）。

**消息来源**：`conversation_detail.messages[]` 是 `conversations.messages[]` 的**翻译+标注产物**，不是独立数据管道：

```
conversations 对话完成
  → 复盘流程读 conversations.messages[]
  → 逐句翻译成 conversation_detail.MessageItem（添加 speaker_role/speaker_ref）
  → AI 回填 annotation（keywords/emotion/intent/responds_to_seq）
  → 写入 conversation_detail
```

两者是本体的**导出关系**，不是重复写入。`conversations.messages[]` 是"演出记录"，`conversation_detail.messages[]` 是"可标注分析的事实"。后者在写入前可能只含部分 annotation（AI 可后续补填）。

---

## 5a. MongoDB · conversations（对话主文档，已有实现）

**定位**：对话会话的主文档，存储模拟/复盘对话的完整消息流与 AI 评分。集合名 `conversations`。

**Evaluation 内嵌类**（5 维评分，2026-07 升级自 3 维）：

| Java 字段 | 类型 | 说明 |
|---|---|---|
| clarity | int | 表达清晰度 0–100 |
| logicality | int | 逻辑思辨力 0–100 |
| empathyListening | int | 共情与倾听 0–100 |
| interactivity | int | 互动积极性 0–100 |
| relaxation | int | 情绪松弛度 0–100 |
| comment | String | AI 评语 |
| strengths | List\<String\> | 优点标签 |
| suggestions | List\<String\> | 改进建议标签 |

> **历史迁移**：初版使用 3 维（`expression`, `affinity`, `logic`），已通过计划 `evaluation-data-structure` 全量升级为 5 维。旧字段已删除（不保留 `@Deprecated`），旧对话数据已在升级前通过 `mongodump` 备份至 `.omo/backup/`。

**Java 实现**：`com.backend.document.ConversationDocument`，`@Document("conversations")`。AI 评分写入路径 → `SimulationService.scoreConversation()` 调用 `ConversationStore.saveScore()` 持久化。解析使用宽容模式（`getIntSafe(key, 50)` + `log.warn`），AI 漏返回某维度时默认 50 分，不抛异常。

---

## 5b. MongoDB · user_profile（用户能力画像，已有实现）

**定位**：用户跨会话的 5 维均值画像，由 `UserProfileTools.assessAbility()` 在每次评分完成后自动触发（`@EventListener(ScoringCompleted.class)`）。

**Java 实现**：`com.backend.document.UserProfileDocument`，`@Document("user_profile")`。

| 字段 | 类型 | 说明 |
|---|---|---|
| userId | Long | 用户 ID |
| avgClarity | int | 清晰度均值（/5.0 平均） |
| avgLogicality | int | 逻辑思辨力均值 |
| avgEmpathyListening | int | 共情倾听均值 |
| avgInteractivity | int | 互动性均值 |
| avgRelaxation | int | 松弛感均值 |
| overallScore | int | 综合分 = `(五维和) / 5.0` |
| scoreRecords | List\<ScoreRecord\> | 历次评分快照数组 |

**ScoreRecord 内嵌类**：

| 字段 | 类型 | 说明 |
|---|---|---|
| sessionId | String | 会话 ID |
| theme | String | 对话主题 |
| score | int | 该次综合分 |
| clarity / logicality / empathyListening / interactivity / relaxation | int | 5 维分 |
| scoredAt | LocalDateTime | 评分时间 |

> **历史迁移**：初版使用 3 维（`avgExpression`, `avgAffinity`, `avgLogic`），已升级为 5 维。`overallScore` 计算公式从 `/3.0` 改为 `/5.0`。

---

## 6. 跨库写入流程与一致性

**写入顺序（先 MySQL 后 Mongo）**：
1. 应用层预生成 `biz_id`；
2. 开 **一个 MySQL 事务**：`INSERT interaction_meta`(带 biz_id, status=0) → `INSERT evaluation_record`(interaction_id=meta.id) → `UPDATE contact SET intimacy_score=?, last_contact_time=? WHERE id=?` → **COMMIT**；
3. COMMIT 成功后用 `biz_id` 去 Mongo **upsert** 明细（幂等）；成功置 `process_status=1`，失败置 `2`。

> 顺序理由：MySQL 先成时，不一致方向是"MySQL 有、Mongo 缺"，可由 `process_status` 扫描重试；若反过来先写 Mongo，回滚会产生难发现的孤儿文档。

**两种写入模式**：
- **real 复盘**：MySQL 事务 commit 后整文档 upsert（含完整 messages）。
- **simulation**：模拟开始建 meta(status=0)+Mongo 空文档；每轮 `$push` 一条 message；结束时回填 `ended_at` + 写 evaluation + upsert 最终标注 + status=1。

**补偿任务**：定时扫 `process_status IN (0,2)`，用 `biz_id` 重发 Mongo upsert，成功置 1。**不上 MQ / 分布式事务**（demo 级足够）。

**关键纪律**：
- `contact.intimacy_score` 与 `last_contact_time` 必须与 evaluation **同 MySQL 事务**更新（关系预警不滞后的命门）。
- `contact.last_contact_time` = 该次 `ended_at`；衰减查询统一用 `COALESCE(ended_at, started_at)`。
- 跨库无单一事务，前端展示容忍秒级不一致。

---

## 7. 亲密度计算口径（写 E 的依据，配套说明）

亲密度 = f(时效, 频率, 深度, 质量)，**按 contact 聚合**，按场景区分数据源：

| 场景 | 时效 | 频率 | 深度 / 质量 数据源 |
|---|---|---|---|
| one_on_one | meta.ended_at | count meta | **evaluation 五维**（1 对 1 时用户级=对子级） |
| salon + 有说话人归属 | meta.ended_at | count meta（按 participant_contact_ids 展开） | **conversation_detail 逐句聚合**（user 句 ↔ 该 contact 句 构成互动对），**不可用 evaluation** |
| salon + 无归属（降级） | meta.ended_at | count meta | **不贡献**（仅时效+频率） |
| simulation | — | — | **不计亲密度** |

> ⚠️ 当前实现态：1v1 深度/质量读 `conversations.Evaluation`（MongoDB，唯一已实现写源）。待 §9.4 收敛计划①（evaluation_record 写入）完成后，切换为读 `evaluation_record`。
> ⚠️ salon 分支当前限制：`conversation_detail` 的翻译+标注写入链路未实现（仅定义了文档结构，无调度/写入代码），E 沙龙分支第一版只能走**降级模式**（仅时效+频率，不贡献深度/质量分）。待翻译链路实现后启用逐句聚合。

**时效衰减**：第一版用 **floor**（衰减到下限不清零，保证老友不因久未联系归零）；历史底蕴项留封测评估。衰减率按 `category` 区分（熟人衰减快、亲密关系抗衰减）。

**预警规则（规则引擎，不用大模型）**：沉寂 / 滑落 / 质量 三类；**只预警二环及以上**；每日封顶（如 3 条）；`warning_dismissed_at` 冷却期内跳过。

> ⚠️ TODO（写 E 时处理）：沙龙逐句聚合的"对子深度/质量"与 one_on_one 的 evaluation 整篇分**量纲/标尺可能不一致**，混入同一公式前须做标尺校准或两源分别归一化。
> ⚠️ 沙龙"有归属/无归属"取决于沟通辅助输入形态（**挂起，见 §8**）；schema 已兼容两种。

---

## 8. 挂起项与占位策略（vibe coding 不阻塞）

下列项**尚未定稿**，但给出占位策略，建表/造 mock/前端渲染可直接跑通，定稿后仅改词表/常量，不改表结构：

| # | 挂起项 | 占位策略（当前可用） | 定稿后改动 |
|---|---|---|---|
| 1 | `self_state` 状态标签词表 | 前端可不传，列存 NULL；mock 可填 `"relaxed"` 等字符串 | 仅约束枚举值 |
| 2 | `category` 关系分类（图谱扇形） | DDL 已 `DEFAULT 'other'`；mock 可用 `other`；前端 mock 已分配 4 类假值（科幻文学/历史人文/推理悬疑/科技商业） | 仅补词表常量 |
| 3 | 隐私部署口径（申报书 7.5 与云端架构的张力） | 默认按**云端**推进（预警定时任务依赖云端）；后端走 repository/service 层抽象，勿写死查云 MySQL | 仅改 service 实现 + 答辩话术 |
| 4 | 沟通辅助输入形态（文字/语音 + 说话人分离）→ 连带决定沙龙多人录入与亲密度沙龙分支 | schema 已兼容（`speaker_ref` 可 unknown、`participant_contact_ids` 可空）；E 沙龙分支先降级 | 仅补 E 沙龙聚合逻辑 |

**环阈值 / 预警阈值 / 衰减参数**：均为**代码常量**，封测校准，不入表。

---

## 9. 技术实现记录

### 9.1 双数据源配置

Spring Boot 3.5.14 同时连接 MongoDB + MySQL：

- **MongoDB**：`spring-boot-starter-data-mongodb`（已有），repo 扫描 `com.backend.repository`
- **MySQL**：`spring-boot-starter-data-jpa` + `mysql-connector-j`（新增），repo 扫描 `com.backend.repository.jpa`
- 注解：`@EnableMongoRepositories(basePackages = "com.backend.repository")` + `@EnableJpaRepositories(basePackages = "com.backend.repository.jpa")`
- `@Primary` DataSource 标注 MySQL 数据源

### 9.2 关键文件索引

| 文件 | 说明 |
|---|---|
| `backend/sql/schema.sql` | 3 表 DDL（evaluation_record, contact, interaction_meta） |
| `backend/…/entity/EvaluationRecord.java` | 评估记录 JPA Entity |
| `backend/…/entity/ContactEntity.java` | 书友档案 JPA Entity（含修订字段） |
| `backend/…/entity/InteractionMeta.java` | 交流元数据 JPA Entity |
| `backend/…/repository/jpa/*Repository.java` | 3 个 JPA Repository |
| `backend/…/document/ConversationDocument.java` | 对话主文档（含 5D Evaluation） |
| `backend/…/document/ConversationDetailDocument.java` | 对话逐句明细文档 |
| `backend/…/document/UserProfileDocument.java` | 用户能力画像（含 5D ScoreRecord） |
| `backend/…/service/ContactService.java` | MongoDB→MySQL contact 同步服务 |
| `backend/…/listener/ContactSyncListener.java` | 联系人事件监听（grace 降级） |
| `frontend/src/types/simulation.ts` | 前端 EvaluationSummary 5D 类型定义 |
| `frontend/src/types/relation.ts` | 前端 RelationContact 类型（含 category） |
| `frontend/src/data/mockRelations.ts` | 前端 mock 书友数据（7 条，含 category） |

### 9.3 3D → 5D 升级历史

| 阶段 | 旧字段 (3D) | 新字段 (5D) |
|---|---|---|
| `ConversationDocument.Evaluation` | expression, affinity, logic | clarity, logicality, empathyListening, interactivity, relaxation |
| `UserProfileDocument` avg | avgExpression, avgAffinity, avgLogic | avgClarity, avgLogicality, avgEmpathyListening, avgInteractivity, avgRelaxation |
| `UserProfileDocument.ScoreRecord` | expression, affinity, logic | clarity, logicality, empathyListening, interactivity, relaxation |
| `SessionSummary.EvaluationSummary` | expression, affinity, logic | clarity, logicality, empathyListening, interactivity, relaxation |
| `SimulationService.getHistory()` map | expression/affinity/logic 3 键 | clarity/logicality/empathyListening/interactivity/relaxation 5 键 |
| AI prompt (`scoreConversation`) | 表达力/亲和力/逻辑性 3 维 | 清晰度/逻辑思辨力/共情倾听/互动积极性/情绪松弛度 5 维 |
| `UserProfileTools.assessAbility()` | `/3.0` 平均 | `/5.0` 平均 |
| Agent system prompts | 表达力/亲和力/逻辑性 | 5 维术语（SimulationAgent/UserProfileAgent/CommAssistAgent） |
| 前端 chat-history.vue | 3 条进度条 | 5 条进度条（cyan/purple/rose/amber/emerald） |

### 9.4 当前写路径差异与收敛方向

**"同一次评分 5 维分，当前落三处"**，下表标清哪些已实现、哪些未实现、目标收敛方向：

| 写入目标 | 触发时机 | 写逻辑状态 | 目标角色 |
|---|---|---|---|
| `conversations.Evaluation` (现有 MongoDB) | `scoreConversation()` 完成 → `ConversationStore.saveScore()` | ✅ **已实现** | **当前唯一写源**，前端 chat-history 读它 |
| `evaluation_record` (MySQL) | `scoreConversation()` 完成 → 同事务写入 | ❌ **写逻辑未实现** | **目标规范源** (canonical source)，供 SQL 聚合查 |
| `user_profile.ScoreRecord[]` (MongoDB) | `assessAbility()` 异步触发 | ✅ **已实现** | **画像因子快照**，评估报告的输入，非评分副本 |
| `user_profile.avgClarity/…/overallScore` | `assessAbility()` 重算 | ✅ **已实现** | **画像均值**，公式 = `ROUND(AVG(五维))`，口径与 evaluation_record 一致 |

**收敛计划（后续迭代）**：
1. 实现 evaluation_record 的写入，使 MySQL 成为评分事实的规范源
2. 前端雷达图/折线图查询源从 conversations.Evaluation 切到 evaluation_record
3. conversations.Evaluation 保留作为"对话时原始快照"（去范式化缓存），不再作为评分主查询源
4. scoreRecords 维持现状（画像输入，非评分权威源）

---

## 附：四表闭环速记

```
evaluation_record ──interaction_id──▶ interaction_meta ──biz_id──▶ conversation_detail (MongoDB)
   │ 5维(用户维度)                          │ related_contact_id (1v1)
   │ related_contact_id (1v1)               │ participant_contact_ids (沙龙)
   ▼ 雷达图/折线图                          ▼
   用户能力                          contact ◀── intimacy_score / last_contact_time
                                     关系图谱/预警/通讯录
                                               │ name / avatar_url / category / relation_type
                                               │ personality / interests / labels
                                               ▼ 图谱扇形 + 社交身份 + 性格/兴趣/标签

conversations (MongoDB)               user_profile (MongoDB)
    │ Evaluation: 5维                         │ avgClarity / avgLogicality / … / overallScore
    ▼ 对话主文档                              ▼ AI 能力画像

            亲密度定时任务：按 contact 聚合 meta(时效/频率)
            + 深度质量(1v1读evaluation / 沙龙读逐句)
```
