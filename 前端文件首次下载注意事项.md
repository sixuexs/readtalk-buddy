# readtalk-buddy

前端代码在/frontend里，
1. 第一次下载后用vscode或Hbuilder-X打开该文件，
2. 在终端里使用"pnpm install"或"npm install"安装依赖，
3. 使用"npm/pnpm run dev:mp-weixin"编译，
4. 打开微信开发者工具导入

注意：前端只用下载frontend.zip，因为package.json文件声明了需要安装的依赖，
而它在/frontend目录下，所以在终端里**安装依赖**前，一定要确保路径末尾是/frontend
